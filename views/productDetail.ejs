<section class="spchitiet">
     <div style="margin-left: 170px;" class="container ">

        <div   style="background-color: rgb(215, 215, 212); height: 400px; width: 900px; " class="row">
            <div style="margin-top: 100px; margin-left: 100px;" class="col-md-4 ">
                 <img src="<%= data.products.image %>" alt="">
            </div>
            <div style="margin-top: 40px;" class="col-md-6">
                <div class="container my-3">
                    <h1><%= data.products.username %></h1>
                    <!-- Star icons using Font Awesome -->
                    <i class="fa fa-star text-warning"></i>
                    <i class="fa fa-star text-warning"></i>
                    <i class="fa fa-star text-warning"></i>
                    <i class="fa fa-star text-warning"></i>
                    <i class="fa fa-star"></i>
                    <h3 style="color: red; "><%= data.products.price %></h3> <samp>vnd</samp></h3>
                    <p><b><%= data.products.description %></b> </p>
                    <div class="mb-3">
                        <label for="quantity" class="form-label">Số lượng đặt mua:</label>
                        <input type="number" class="form-control" id="quantity" name="quantity" min="1" value="1">
                        <button style="width: 210px;" type="submit" class="btn btn-warning">Thêm vào giỏ hàng</button>
                        <button style="width: 70px;" type="submit" class="btn btn-warning"><i class="fa fa-heart white-heart"></i></button>

                    </div>
                </div>   
            </div>
        </div>
    </div>
</section>
<style>
    /* CSS cho Navbar */
.navbar {
  background-color: #333; /* Màu nền */
  overflow: hidden; /* Loại bỏ các phần tử con vượt ra khỏi khung */
}

.navbar a {
  float: left; /* Dàn menu theo chiều ngang */
  display: block; /* Hiển thị các phần tử a như các khối */
  color: white; /* Màu chữ */
  text-align: center; /* Căn giữa văn bản */
  padding: 14px 16px; /* Khoảng cách giữa các phần tử */
  text-decoration: none; /* Loại bỏ đường viền dưới */
}
.condible{
  
}

.navbar a:hover {
  background-color: #ddd; /* Màu nền khi hover */
  color: black; /* Màu chữ khi hover */
}

/* CSS cho Nội dung */
.content {
  padding: 16px;
}

/* CSS cho Footer */
.footer {
  background-color: #333; /* Màu nền */
  color: white; /* Màu chữ */
  text-align: center; /* Căn giữa văn bản */
  padding: 16px 0; /* Khoảng cách giữa nội dung và footer */
}
.conty{
  font-style: italic;
  font-weight: bold;
  color: rgb(202, 9, 122);
  font-size: 55px;
  margin-left: -170px;
}
.shop{
  font-size: 30px;
  margin-top:-30px ;
  margin-left: 140px;
  color: rgb(38, 0, 255);
}
.abc{
  position: absolute; 
  margin-top: -280px;
  margin-left: 620px;
  width: 100px;
}
/*  singup */
/* Form styling */
form {
  border: 3px solid #f1f1f1;
  max-width: 600px; /* Set maximum width for the form */
  width: 100%; /* Set width to 90% of the viewport */
  margin: 0 auto; /* Center the form horizontally */
  padding: 15px; /* Reduce padding */
}

/* Full-width inputs */
input[type=text], input[type=password], input[type=email], input[type=date], input[type=tel] {
  width: 100%;
  padding: 10px; /* Reduce padding */
  margin: 5px 0;
  border: 1px solid #ccc;
  box-sizing: border-box;
}

/* Set a style for all buttons */
button {
  background-color: #04AA6D;
  color: white;
  padding: 10px 15px; /* Reduce padding */
  margin: 5px 0;
  border: none;
  cursor: pointer;
  width: 100%;
}

/* Add a hover effect for buttons */
button:hover {
  opacity: 0.8;
}

/* Extra style for the cancel button (red) */
.cancelbtn {
  background-color: #f44336;
}

/* Center the buttons */
.cancelbtn, .signupbtn {
  float: none;
  width: auto;
}

/* Add padding to containers */
.container {
  padding: 10px; /* Reduce padding */
}

/* Clear floats */
.clearfix::after {
  content: "";
  clear: both;
  display: table;
}
/*  menu ẩn */
/* Style the dropdown button */
.dropbtn {
  background-color: #4CAF50;
  color: white;
  padding: 16px;
  font-size: 16px;
  border: none;
  cursor: pointer;
}

/* Style the dropdown content (hidden by default) */
.dropdown-content {
  display: none;
  position: absolute;
  background-color: #f9f9f9;
  min-width: 160px;
  box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
  z-index: 1;
}

/* Style the links inside the dropdown */
.dropdown-content a {
  color: black;
  padding: 12px 16px;
  text-decoration: none;
  display: block;
}

/* Change color of dropdown links on hover */
.dropdown-content a:hover {background-color: #f1f1f1;}

/* Show the dropdown menu on hover */
.dropdown:hover .dropdown-content {display: block;}

/* Change the background color of the dropdown button when the dropdown content is shown */
.dropdown:hover .dropbtn {background-color: #3e8e41;}
.example{
  margin-top: -24px;
  margin-left: 20px;
  border-radius: 20px;
}
form.example input[type=text] {
  padding: 10px;
  font-size: 17px;
  
  float: left;
  width: 80%;
 
}

form.example button {
  float: left;
  width: 20%;
  padding: 10px;
  background: #2196F3;
  color: white;
  font-size: 17px;
  
  
  
}

form.example button:hover {
  background: #0b7dda;
}
.list-unstyled li{
  color: #070707;
}
.product {
  padding-top: 20px;
  background: #f8f9fa;
}

.product-card {
  width: 250px;
  box-shadow: 0 4px 8px rgba(0,0,0,0.1);
  transition: 0.3s;
  border-radius: 10px; /* Rounded corners for the card */
}

.product-card:hover {
  box-shadow: 0 8px 16px rgba(0,0,0,0.2);
}

.card-img-top {
  height: 200px;
  width: 100%;
  object-fit: cover; /* Ensures the image covers the area without being stretched */
  border-top-left-radius: 10px; /* Rounded corners for the top of the image */
  border-top-right-radius: 10px;
}

.card-body {
  padding: 15px; /* Padding inside the card */
}

.product-title {
  font-size: 18px;
  margin-bottom: 10px; /* Space below the product title */
}

.product-description {
  color: #666;
  font-size: 14px;
  margin-bottom: 15px; /* Space below the description before the button */
}

.btn-primary {
  background-color: #007bff; /* Custom color for the button */
  border: none;
  padding: 8px 16px;
  transition: background-color 0.3s;
}

.btn-primary:hover {
  background-color: #0056b3; /* Darker shade on hover */
}





</style>