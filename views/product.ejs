<section class="product">
    <div class="container mt-3">
      <div class="p-3 text-center">
        <h1>Sản phẩm của chúng tôi</h1><br>
      </div>
         
      <div class="row" style="margin-left: 40px; ">
        <% data.products.forEach(element => { %>
          <div class="col-md-3 mb-4">
              <div <%= element.id %> class="card tong" style="width:250px;object-fit: cover;">
           <img  class="card-img-top product" src="<%= element.image %>" alt="Card image" style="height: 180px; width: 140px; margin-left: 50px;">
                  <div class="card-body">
                    <h4><%= element.username %></h4>
                    <p style="color: red;"><b><%= element.price %></b></p>
                    <a href="/getproductDetail/<%= element.id %>" class="btn btn-primary">Chi Tiết</a>
                  </div>
                </div>
          </div>
          <% }) %>  
        </div>
    </div>
    
        </section>